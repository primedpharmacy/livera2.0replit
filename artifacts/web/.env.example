# Google Maps JavaScript API key for the intake form address autocomplete.
# Must be exposed to the browser, so it uses the NEXT_PUBLIC_ prefix.
# Enable the "Maps JavaScript API" and "Places API" in Google Cloud Console
# and restrict the key by HTTP referrer to your deployed domains.
NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=
